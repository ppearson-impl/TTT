{
  "version": 2,
  "name": "pmo-agent",
  "public": "pmo-agent",
  "buildCommand": "echo 'No build needed'",
  "outputDirectory": "pmo-agent",
  "routes": [
    { "src": "/api/(.*)", "dest": "/pmo-agent/api/$1.js" },
    { "src": "/(.*)", "dest": "/pmo-agent/index.html" }
  ]
}
